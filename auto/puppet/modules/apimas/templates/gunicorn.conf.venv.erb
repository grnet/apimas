CONFIG = {
    'mode': 'wsgi',
    'environment': {
        'PYTHONHOME': '<%= @srv_root %>/<%= @api_name %>-server/venv/',
    },
    'working_dir': '<%= @srv_root %>/<%= @api_name %>-data/',
    'user': '<%= @api_name %>',
    'group': '<%= @api_name %>',
    'args': (
        '--log-level=debug',
        '--log-file=<%= @srv_root %>/<%= @api_name %>-data/gunicorn.log',
        '--bind=<%= @app_address %>',
        '--workers=<%= @app_workers %>',
        '--reload',
        #'--worker-class=egg:gunicorn#sync',
        # '--timeout=30',
        '<%= @wsgi_app %>',
    ),
}
